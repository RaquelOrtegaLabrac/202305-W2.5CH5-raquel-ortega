const rows = 4;
const columns = 3;
const array = [];

const createBoard = (rows, columns) => {
  for (let index = 0; index < columns; index++) {
    array[index];
    for (let j = 0; index < rows; index++) {
      array[0];
    }
  }
};
